service cloud.firestore {
  match /databases/{database}/documents {
    match /races/{race} {
      // make sure auth.uid is present and the rule actually works
      allow read, write: if request.auth.uid === resource.data.userId
    }
  }
}
