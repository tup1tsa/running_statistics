import * as React from 'react';
import './App.css';
import { FileSelector } from './sandbox/FileSelector';

class App extends React.Component {
  render() {
    return (
     <FileSelector />
    );
  }
}

export default App;
